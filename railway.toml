# =============================
# 🚀 Railway Laravel Nixpacks
# =============================

[build]
builder = "NIXPACKS"
version = "2"   # أي تغيير هنا = Clear Cache إجباري

[build.nixpacks]
phases = ["php", "node"]

